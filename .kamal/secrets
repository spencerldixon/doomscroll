# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Example of extracting secrets from 1password (or another compatible password manager)
# SECRETS=$(kamal secrets fetch --adapter 1password --account your-account --from Vault/Item KAMAL_REGISTRY_PASSWORD SECRET_KEY_BASE DOOMSCROLL_DATABASE_PASSWORD)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
# SECRET_KEY_BASE=$(kamal secrets extract SECRET_KEY_BASE ${SECRETS})
# DOOMSCROLL_DATABASE_PASSWORD=$(kamal secrets extract DOOMSCROLL_DATABASE_PASSWORD ${SECRETS})

# Use a GITHUB_TOKEN if private repositories are needed for the image
# GITHUB_TOKEN=$(gh config get -h github.com oauth_token)

# Load local production deploy values. This file is ignored by git.
if [ -f .env.production.local ]; then
  set -a
  . ./.env.production.local
  set +a
fi

# Grab secrets from ENV
# KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
SECRET_KEY_BASE=$SECRET_KEY_BASE
DOOMSCROLL_DATABASE_PASSWORD=$DOOMSCROLL_DATABASE_PASSWORD
POSTGRES_PASSWORD=$DOOMSCROLL_DATABASE_PASSWORD
SELF_HOSTED=$SELF_HOSTED
ENABLE_REGISTRATION=$ENABLE_REGISTRATION
MAILER_DEFAULT_URL_HOST=$MAILER_DEFAULT_URL_HOST
MAILER_DEFAULT_URL_PORT=$MAILER_DEFAULT_URL_PORT
MAILER_DEFAULT_FROM=$MAILER_DEFAULT_FROM
SMTP_ADDRESS=$SMTP_ADDRESS
SMTP_PORT=$SMTP_PORT
SMTP_DOMAIN=$SMTP_DOMAIN
SMTP_USERNAME=$SMTP_USERNAME
SMTP_PASSWORD=$SMTP_PASSWORD
GOOGLE_OAUTH2_CLIENT_ID=$GOOGLE_OAUTH2_CLIENT_ID
GOOGLE_OAUTH2_CLIENT_SECRET=$GOOGLE_OAUTH2_CLIENT_SECRET
TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN
TELEGRAM_CHAT_ID=$TELEGRAM_CHAT_ID

# Optional integrations
# SMTP_PASSWORD=$SMTP_PASSWORD
# GOOGLE_OAUTH2_CLIENT_SECRET=$GOOGLE_OAUTH2_CLIENT_SECRET
# TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN
# TELEGRAM_CHAT_ID=$TELEGRAM_CHAT_ID
